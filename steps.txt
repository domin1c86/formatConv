已创建 [README_zh.md](D:/Coding/formatConv/README_zh.md)。PowerShell 输出中文会乱码，但我已用 UTF-8 读取确认文件内容正常。

**自行测试方案**
**1. 环境确认**

```powershell
flutter --version
dart --version
go version
```

确认：

```text
Flutter 3.44.2
Dart SDK 3.12.2
Go 1.21+
```

**2. 清理残留进程**

```powershell
Get-Process | Where-Object { $_.ProcessName -match 'flutter|dart|dartvm|frontend_server|gen_snapshot' } | Stop-Process -Force
```

**3. 依赖和静态检查**

```powershell
cd D:\Coding\formatConv\apps
flutter pub get
flutter analyze
```

**4. 单元测试**

```powershell
cd D:\Coding\formatConv\apps
flutter test
```

Go 测试：

```powershell
cd D:\Coding\formatConv\native
go test ./...
```

**5. Windows 构建**

```powershell
cd D:\Coding\formatConv\apps
.\scripts\build_windows.ps1 -Mode release
```

构建失败后：
```powershell
cd D:\Coding\formatConv\apps
Remove-Item -Recurse -Force .\build -ErrorAction SilentlyContinue
.\scripts\build_windows.ps1 -Mode release
```

检查产物：

```powershell
Test-Path .\build\windows\x64\runner\Release\format_conv.exe
Test-Path .\build\windows\x64\runner\Release\format_conv.dll
```

都应返回 `True`。

**6. 实际软件体验测试**

启动：

```powershell
cd D:\Coding\formatConv\apps
.\build\windows\x64\runner\Release\format_conv.exe
```

重点检查：

- 顶部黑色导航显示正常。
- `English / 中文` 切换可用，主要界面文案会切换。
- 主界面符合 `DESIGN.md` 风格：黑色导航、浅灰背景、白色工作区、深色文件选择区、蓝色主按钮。
- 拖拽文件到选择区可识别。
- 点击浏览文件可选择多个文件。
- 选择格式后能开始转换。
- 无损/有损、质量滑条、编码器选项交互正常。
- 转换进度条显示稳定。
- 转换完成后结果区显示成功/失败状态。
- 成功结果点击打开按钮能打开输出文件。

**7. 转换功能样例**

建议准备小体积文件测试：

```text
PNG -> JPEG
JPEG -> WebP
MP3 -> WAV
MP4 -> WebM
MP4 -> MKV
```

每个用例检查：

- 输出文件生成在源文件同目录。
- 不勾选覆盖时，重名文件会自动加后缀。
- 勾选覆盖时，会覆盖目标输出。
- 转换失败时错误提示可读。

**8. 高 DPI / 体验专项**

在 Windows 显示设置中分别测试：

```text
100%
125%
150%
200%
```

检查：

- 字体清晰，不模糊。
- 卡片没有被 4K 屏拉得过宽。
- 小窗口下布局变为单列。
- 按钮、进度条、文件名不溢出。
- 从高 DPI 显示器拖到普通显示器，窗口布局不乱。

如果有失败，保留三样东西即可：执行命令、完整错误输出、截图。